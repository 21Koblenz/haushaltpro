name: Release v0.21.8

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/release-v0.21.8.yml

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Validate workflow
        run: echo "v0.21.8 release workflow is valid"
